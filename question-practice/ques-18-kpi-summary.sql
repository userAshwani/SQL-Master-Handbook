-- QUESTION: Write a single-row aggregate query returning total revenue,
-- total order count, average order value, and count of distinct
-- customers who have placed an order — a KPI dashboard summary.
-- Used by pro-4-analytics-engine to practice aggregate KPI summaries.
--
-- Sample table(s): customers, orders, order_items
-- Input:  the sample tables
-- Output: one row: (total_revenue, order_count, avg_order_value, distinct_customers)

-- TODO: write your query below


-- --- TEST ---
-- Confirm avg_order_value roughly equals total_revenue / order_count on
-- the sample data.
