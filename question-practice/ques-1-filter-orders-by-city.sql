-- QUESTION: Select every order placed by customers who live in a given
-- city, sorted by order_date descending (most recent first).
-- Used by pro-1-sql-fundamentals to practice SELECT/WHERE/ORDER BY.
--
-- Sample table(s): customers, orders — see projects/pro-1-sql-fundamentals/schema.sql
-- Input:  city = 'Pune'
-- Output: rows from orders joined to customers, city = 'Pune', newest order first

-- TODO: write your query below


-- --- TEST ---
-- Run this against the schema.sql sample data with city = 'Pune' and
-- confirm Asha Rao's two orders come back, order_id 2 before order_id 1.
