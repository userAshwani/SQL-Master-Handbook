-- Q8 (Easy): Create a VIEW exposing only active accounts (a row-level
-- filtering view using WHERE is_active = 1).
-- Sample table: accounts(account_id, holder_name, balance, is_active)
-- Input:  the sample accounts table
-- Output: a view active_accounts with only is_active = 1 rows

-- TODO: write your CREATE VIEW statement below


-- --- TEST ---
-- Confirm no row in active_accounts has is_active = 0.
