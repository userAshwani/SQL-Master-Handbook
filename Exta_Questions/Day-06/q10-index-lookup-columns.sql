-- Q10 (Medium): Write CREATE INDEX statements to speed up lookups on
-- appointments by patient_id and by appointment_date.
-- Target table: appointments(appointment_id, patient_id, doctor_id,
--   appointment_date, status)
-- Input:  the appointments table
-- Output: two indexes: one on patient_id, one on appointment_date

-- TODO: write your CREATE INDEX statements below


-- --- TEST ---
-- Query sqlite_master (or your engine's equivalent) and confirm both
-- indexes exist on the appointments table.
