-- Q7 (Medium): Return the 3rd page of books, 10 per page, ordered by
-- book_id, using LIMIT/OFFSET.
-- Sample table: books(book_id, title, author_id, genre, price, published_year, stock)
-- Input:  page = 3, page_size = 10
-- Output: rows 21-30 when ordered by book_id

-- TODO: write your query below


-- --- TEST ---
-- Confirm exactly 10 rows are returned (or fewer if the table is
-- smaller) and the first row is the 21st book by book_id order.
