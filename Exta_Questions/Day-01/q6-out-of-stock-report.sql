-- Q6 (Easy): Select all books that are out of stock — stock IS NULL or
-- stock = 0.
-- Sample table: books(book_id, title, author_id, genre, price, published_year, stock)
-- Input:  the books table
-- Output: rows where stock is missing or zero

-- TODO: write your query below


-- --- TEST ---
-- Confirm every returned row has stock = 0 or a NULL stock value, and no
-- row with stock > 0 is returned.
