-- Q3 (Easy): Select all books whose title starts with "The" using LIKE.
-- Sample table: books(book_id, title, author_id, genre, price, published_year, stock)
-- Input:  title LIKE 'The%'
-- Output: only rows whose title begins with "The"

-- TODO: write your query below


-- --- TEST ---
-- Confirm every returned title starts with the literal text "The".
