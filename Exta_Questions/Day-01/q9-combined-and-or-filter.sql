-- Q9 (Medium): Select books where (genre = 'Fiction' AND price < 15) OR
-- (genre = 'Nonfiction' AND price < 20) — practice combining AND/OR with
-- parentheses correctly.
-- Sample table: books(book_id, title, author_id, genre, price, published_year, stock)
-- Input:  the books table
-- Output: rows matching either condition group

-- TODO: write your query below


-- --- TEST ---
-- Confirm no returned row is Fiction priced >= 15, and no returned row
-- is Nonfiction priced >= 20.
