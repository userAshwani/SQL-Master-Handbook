-- Q10 (Easy): Select title, price, and a computed discounted_price
-- column equal to price * 0.9 (a 10% discount), rounded to 2 decimals.
-- Sample table: books(book_id, title, author_id, genre, price, published_year, stock)
-- Input:  the books table
-- Output: rows of (title, price, discounted_price)

-- TODO: write your query below


-- --- TEST ---
-- Confirm discounted_price is always less than price for every row.
