-- Q6 (Easy): Compute the minimum and maximum single movement quantity
-- per product category.
-- Sample tables: products(product_id, name, category), stock_movements(
--   movement_id, product_id, quantity, movement_type, movement_date)
-- Input:  the sample tables
-- Output: rows of (category, min_quantity, max_quantity)

-- TODO: write your query below


-- --- TEST ---
-- Confirm min_quantity <= max_quantity for every row.
