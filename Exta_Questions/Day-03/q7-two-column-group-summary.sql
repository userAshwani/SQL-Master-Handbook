-- Q7 (Medium): Group by two columns (category, movement_type) to produce
-- a summary report of total quantity for each combination.
-- Sample tables: products(product_id, name, category), stock_movements(
--   movement_id, product_id, quantity, movement_type, movement_date)
-- Input:  the sample tables
-- Output: rows of (category, movement_type, total_quantity)

-- TODO: write your query below


-- --- TEST ---
-- Confirm no (category, movement_type) combination appears more than
-- once in the result.
