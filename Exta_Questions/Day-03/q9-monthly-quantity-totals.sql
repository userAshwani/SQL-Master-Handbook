-- Q9 (Medium): Compute the total quantity moved per month using a
-- date-grouping expression (e.g. strftime('%Y-%m', movement_date)).
-- Sample table: stock_movements(movement_id, product_id, quantity,
--   movement_type, movement_date)
-- Input:  the sample stock_movements table
-- Output: rows of (year_month, total_quantity)

-- TODO: write your query below


-- --- TEST ---
-- Confirm each year_month value is formatted as 'YYYY-MM' and rows are
-- ordered chronologically.
