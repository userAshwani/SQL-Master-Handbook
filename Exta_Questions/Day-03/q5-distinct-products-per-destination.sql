-- Q5 (Medium): Count the number of distinct products shipped to each
-- destination.
-- Sample tables: shipments(shipment_id, destination, ship_date),
--   shipment_items(shipment_id, product_id, quantity)
-- Input:  the sample tables
-- Output: rows of (destination, distinct_product_count)

-- TODO: write your query below


-- --- TEST ---
-- Confirm distinct_product_count never exceeds the total number of
-- products in the catalog.
