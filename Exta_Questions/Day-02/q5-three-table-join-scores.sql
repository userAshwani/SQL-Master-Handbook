-- Q5 (Medium): Join students, enrollments and courses together to show
-- student name, course title, and score in one report.
-- Sample tables: students(student_id, name, grade), courses(course_id,
--   title, credits), enrollments(enrollment_id, student_id, course_id, score)
-- Input:  the sample tables
-- Output: rows of (student_name, course_title, score)

-- TODO: write your query below


-- --- TEST ---
-- Confirm the row count equals the number of rows in enrollments.
