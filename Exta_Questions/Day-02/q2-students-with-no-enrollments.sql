-- Q2 (Easy): LEFT JOIN students to enrollments to find every student who
-- has zero enrollments.
-- Sample tables: students(student_id, name, grade), enrollments(enrollment_id,
--   student_id, course_id, score)
-- Input:  the sample tables
-- Output: rows of student names whose enrollment_id came back NULL

-- TODO: write your query below


-- --- TEST ---
-- Confirm every returned student truly has no row in enrollments.
