-- Q1 (Easy): INNER JOIN students and enrollments (and courses) to list
-- each student's name alongside the titles of courses they're enrolled in.
-- Sample tables: students(student_id, name, grade), courses(course_id,
--   title, credits), enrollments(enrollment_id, student_id, course_id, score)
-- Input:  the sample tables
-- Output: rows of (student_name, course_title)

-- TODO: write your query below


-- --- TEST ---
-- Confirm a student with zero enrollments does not appear in the result.
