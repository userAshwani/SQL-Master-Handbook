-- Q4 (Medium): CROSS JOIN a grades lookup table and a sections lookup
-- table to generate every possible grade/section combination.
-- Sample tables: grades(grade_level), sections(section_name)
-- Input:  3 grades x 3 sections
-- Output: 9 rows, one per (grade_level, section_name) combination

-- TODO: write your query below


-- --- TEST ---
-- Confirm the row count equals (number of grades) * (number of sections).
