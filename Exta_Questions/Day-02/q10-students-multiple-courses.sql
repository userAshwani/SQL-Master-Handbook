-- Q10 (Medium): Join enrollments to students, group by student, and
-- return only students enrolled in more than one course.
-- Sample tables: students(student_id, name, grade), enrollments(enrollment_id,
--   student_id, course_id, score)
-- Input:  the sample tables
-- Output: rows of (student_name, course_count) where course_count > 1

-- TODO: write your query below


-- --- TEST ---
-- Confirm no returned row has course_count <= 1.
